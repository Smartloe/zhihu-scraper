# Scraping Results / 抓取结果

### 📁 目录说明 (Directory Overview)
此目录用于存储知乎抓取出的结构化数据（Markdown、图片及本地数据库）。为了保护隐私和减少仓库体积，此目录下的内容默认**不会**上传到 GitHub。

This directory stores structured data scraped from Zhihu (Markdown, images, and local database). By default, contents here are **ignored** by Git to protect privacy and save space.

### 🔒 边界说明 (Repository Boundary)
以下内容都属于本地运行产物，不应提交到版本库：

- 抓取出的 `index.md` 和 `images/`
- 本地数据库 `zhihu.db`
- 增量监控状态 `.monitor_state.json`

The following items are local runtime artifacts and should stay out of Git:

- Exported `index.md` files and `images/`
- Local database `zhihu.db`
- Incremental monitor state `.monitor_state.json`

---
*Generated by [zhihu-scraper](https://github.com/yuchenzhu-research/zhihu-scraper)*
